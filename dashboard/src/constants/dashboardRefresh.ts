/** Fired from widget fetch() finally when detail.epoch matches the parent refresh cycle. */
export const DASHBOARD_WIDGET_FETCH_DONE = "dashboard-widget-fetch-done";

/** App header manual refresh and global poll; DashboardView and GarageDetailView listen. */
export const DASHBOARD_REQUEST_REFRESH_EVENT = "dashboard-request-refresh";
