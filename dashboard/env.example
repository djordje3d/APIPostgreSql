# API base URL (no trailing slash). Use /api when using Vite proxy to backend.
VITE_API_URL=http://localhost:8000
# Optional: when backend has API_KEY set and you are not using login, put the same value here.
# When using login, the dashboard stores the JWT after sign-in; VITE_API_KEY is not required.
# VITE_API_KEY=your-secret-key
